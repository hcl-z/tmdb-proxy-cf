name = "tmdb-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
# No environment variables needed currently

# Optional: bind a custom domain in the Cloudflare Dashboard
# Workers & Pages → tmdb-proxy → Settings → Domains & Routes
