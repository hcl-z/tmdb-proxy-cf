{
  "name": "tmdb-proxy-cf",
  "version": "1.0.0",
  "description": "TMDB API proxy for Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "wrangler": "^3.0.0"
  }
}
