{
  "name": "tmdb-proxy-cf",
  "version": "1.0.0",
  "description": "TMDB API proxy for Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
